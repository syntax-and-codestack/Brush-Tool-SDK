#<IFNTAG>BRUSHPRIMIT_XMLPP
#<TAG>BRUSHPRIMIT_XMLPP
  
//=========================
//numeric is an integer
// ? means or
//=========================
  numeric Primit_IdBrush = 0 ? 1;
  numeric * nPrimitiveId;

 decimal Primit_PointDistance = 0.5d ? random<d>;

#<ENDTAG>
